# ============================================================================
# SimUCI - Streamlit Secrets Configuration Template
# ============================================================================
# Copy this file to .streamlit/secrets.toml and fill in the values.
# NEVER commit secrets.toml to version control (already in .gitignore).
#
# For deployment on Streamlit Community Cloud, paste these values into
# the "Secrets" section of your app's settings.
# ============================================================================


# --------------------------------------------------------------------------
# Google Drive - Service Account (RECOMMENDED)
# --------------------------------------------------------------------------
# This is the secure way to access data files from Google Drive.
#
# Setup:
#   1. Go to https://console.cloud.google.com
#   2. Create a project (or reuse one) → enable "Google Drive API".
#   3. Go to IAM & Admin → Service Accounts → Create Service Account.
#   4. Download the JSON key file.
#   5. Share the Google Drive folder with the service account email
#      (e.g. simuci-data@your-project.iam.gserviceaccount.com) as Viewer.
#   6. Fill in the values below from the JSON key file.
#
# Security: the service account is a SEPARATE identity. It can only see
# files/folders explicitly shared with it. Your personal Google account
# credentials are never used.
# --------------------------------------------------------------------------


[google_drive]
folder_id = "1nbAUOUQd6x-vDYq3dlrIg6-T_ow7x1oo"

[google_drive.service_account]
type = "service_account"
project_id = "your-gcloud-project-id"
private_key_id = "your-private-key-id"
private_key = "-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
client_email = "your-service-account@your-project.iam.gserviceaccount.com"
client_id = "123456789012345678901"
auth_uri = "https://accounts.google.com/o/oauth2/auth"
token_uri = "https://oauth2.googleapis.com/token"
auth_provider_x509_cert_url = "https://www.googleapis.com/oauth2/v1/certs"
client_x509_cert_url = "https://www.googleapis.com/robot/v1/metadata/x509/your-service-account%40your-project.iam.gserviceaccount.com"

# --------------------------------------------------------------------------
# Google Drive - Legacy fallback (per-file IDs)
# --------------------------------------------------------------------------
# Only needed if you do NOT use the service account method above.
# Requires each file to be shared as "Anyone with the link" (less secure).
#
# [google_drive_files]
# datos_pacientes = "FILE_ID_FROM_GOOGLE_DRIVE_URL"
# fichero_datos = "FILE_ID_FROM_GOOGLE_DRIVE_URL"
# df_centroides = "FILE_ID_FROM_GOOGLE_DRIVE_URL"
# data_with_pred_and_prob = "FILE_ID_FROM_GOOGLE_DRIVE_URL"
